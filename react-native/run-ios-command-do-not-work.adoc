= React Native: Command `run-ios` unrecognized

===== 문제
* `React Native: Command `run-ios` unrecognized`

===== 해결

[source, shell]
----
$ rm -rf node_modules/
$ npm install
$ react-native run-ios
----

